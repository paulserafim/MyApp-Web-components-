import {AppMain} from './AppMain';

//when creating an element, use at least 2 words separated by -
//here we do the associating between the tagname and the class
window.customElements.define('app-main', AppMain);